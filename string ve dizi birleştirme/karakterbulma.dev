#include<stdio.h> 
#include<conio.h> 
#include<string.h> 
int main()
{
   karakter dizisi[100], kelime[20];
   int i, j, ls, lw, temp, sayýW=0, chk, doIncrement;
   printf ( "Dizeyi Giriniz: " );
   alýr (str);
   printf ( "Bir Kelime Giriniz:" );
   alýr (kelime);
   ls = strlen (str);
   lw = strlen (kelime);
   ( i=0; i<ls; i++) için
   {
      sýcaklýk = ben;
      doArtýþ = 0;
      ( j=0; j<lw; j++) için
      {
         if (str[i]==kelime[j])
         {
            if (temp>0 && (temp+lw)<ls)
            {
               if (str[temp-1]== ' ' && str[temp+lw]== ' ' )
                  doArtý=1;
            }
            else if (temp==0 && (temp+lw)<ls)
            {
               if (str[temp+lw]== ' ' )
                  doArtý=1;
            }
            else if (temp>0 && (temp+lw)==ls)
            {
               if (str[temp-1]== ' ' )
                  doArtý=1;
            }
            if (doArtý==1)
               ben++;
            baþka 
               kýrmak ;
         }
      }
      chk = i-temp;
      eðer (chk==lw)
         sayW++;
      ben = sýcaklýk;
   }
   printf ( " \n Oluþum = %d " , sayýW);
   al ();
   0 döndür ;
}
